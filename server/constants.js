const USER_ROLES = {
  ADMIN: 'admin',
  CUSTOMER: 'customer'
};

const ORDER_STATUS = {
  PENDING: 'pending',
  READY: 'ready',
  COMPLETED: 'completed'
};

const CATEGORIES = ['Burgers', 'Fries', 'Drinks', 'Snacks'];

const DISCOUNT_RATE = 0.05;

module.exports = {
  USER_ROLES,
  ORDER_STATUS,
  CATEGORIES,
  DISCOUNT_RATE
};
